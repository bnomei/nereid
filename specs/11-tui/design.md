# Design — 11-tui

Keep UI code in `src/tui/`.

This spec depends on introducing UI dependencies in `02-dependencies/T002`.

Initial UX targets:
- panes: diagram, inspector, xrefs list
- navigation + jump to refs (no hit-testing required initially)

