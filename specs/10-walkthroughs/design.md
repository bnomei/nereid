# Design — 10-walkthroughs

Walkthroughs are cross-cutting and should be tackled after:
- model shells exist (`03-model-core`)
- canvas primitives exist (`06-render-canvas`)
- store exists (`09-session-store`)

Keep the first walkthrough renderer minimal (box + arrow; no advanced layout).

