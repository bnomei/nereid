# Design — 08-query-engine

Keep query primitives in `src/query/`.

Design goals:
- Correctness-first typed queries aligned with `docs/protocol-01.md`.
- Deterministic output ordering for stable tests.

